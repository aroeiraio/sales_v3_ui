# Docker Environment Configuration
# This file contains environment variables for Docker Compose setup

NODE_ENV=production
PORT=8080
HOST=0.0.0.0

# API Configuration for browser access
# Frontend runs in browser and connects to localhost
VITE_API_BASE_URL=http://localhost:8090/interface
VITE_MEDIA_BASE_URL=http://localhost:8090
VITE_WEBSOCKET_URL=ws://localhost:9010

# Application Configuration
VITE_APP_NAME=InoBag Sales V3
VITE_APP_SHORT_NAME=InoBag
VITE_APP_DESCRIPTION=Point of Sale interface for vending machines

# Feature Flags
VITE_ENABLE_CUSTOMER_INFO=true
VITE_ENABLE_SYSTEM_STATUS_CHECK=true

# Security (for containerized setup)
VITE_CORS_ORIGIN=http://localhost:8080
